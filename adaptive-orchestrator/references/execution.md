# Execution
Give workers minimal context: task, parent goal, direct dependency outputs, relevant decisions/files and latest blocking feedback. Prefer isolated Git branch/worktree for code changes. Record append-only run evidence with task/claim/runtime/worker/model, commits, changed artifacts, tests and summary. Workers cannot rewrite frozen criteria to pass themselves.
