# Reviewing
Run deterministic checks first. Evaluate every frozen acceptance criterion. Critics may additionally block regressions, correctness/security/safety defects or explicit project constraints. Keep blocking and non-blocking findings separate. Style preferences and speculative enhancements are non-blocking unless contracted. Bound retries and escalate instead of looping forever.
