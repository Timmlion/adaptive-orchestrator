# Preflight
Before planning or scheduling write `environment/<runtime-id>.json`. Record harness identity and only verified filesystem, shell, browser, vision, image-generation, subagent, parallel-agent, model-selection and model capabilities. Use `unknown` when unverifiable. Distinguish available models from models selectable per worker.
